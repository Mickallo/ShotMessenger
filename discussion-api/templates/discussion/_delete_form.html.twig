<form method="post" action="{{ path('app_discussion_delete', {'uuid': discussion.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discussion.uuid) }}">
    <button class="btn">Delete</button>
</form>
