{% extends 'base.html.twig' %}

{% block title %}Discussion{% endblock %}

{% block body %}
    <h1>Discussion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>uuid</th>
                <td>{{ discussion.uuid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_discussion_index') }}">back to list</a>

    <a href="{{ path('app_discussion_edit', {'uuid': discussion.uuid}) }}">edit</a>

    {{ include('discussion/_delete_form.html.twig') }}
{% endblock %}
