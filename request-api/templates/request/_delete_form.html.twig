<form method="post" action="{{ path('app_domain_entity_request_delete', {'uuid': request.uuid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ request.uuid) }}">
    <button class="btn">Delete</button>
</form>
